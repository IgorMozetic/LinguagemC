#include<stdio.h>
#include<conio.h>

int main ()
{
    int x;
    for( x=0; x<=20; x++)
    {
        if ( x % 2 == 1)
        printf(" %d", x);
    }
        getch ();
        return 0;
}
